name: CI

on:
  pull_request:

jobs:
  python:
    runs-on: ubuntu-latest
    steps:
      - name: Install dependencies
        run: python -m pip install -r requirements.txt
      - name: Run unit tests
        run: python -m pytest scripts/tests
      - name: Run selftest
        run: python scripts/selftest_pipeline.py
